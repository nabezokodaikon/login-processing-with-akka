language: scala
jdk: oraclejdk8
scala:
    - 2.12.7
script:
    - sbt clean coverage test coverageReport
after_success:
    - bash <(curl -s https://codecov.io/bash)
cache:
    directories:
        - $HOME/.ivy2/cache
        - $HOME/.sbt
notifications:
    email: false
